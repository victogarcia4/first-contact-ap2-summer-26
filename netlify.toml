# Netlify deploy config for a Vite (SPA) build.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side anchors/paths resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
